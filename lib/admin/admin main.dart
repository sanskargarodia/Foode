import 'package:flutter/material.dart';
import 'package:food/admin/bottom bar.dart';
class Apple extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "Train Delivery",
      home: Bottom_bar() ,

    );
  }
}